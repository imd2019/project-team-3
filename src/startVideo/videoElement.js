import Sprite from "../sprite.js";

export default class VideoElement extends Sprite {
  constructor(x, y, width, height, video) {
    super(x, y, width, height, video);
  }

  play() {
    // console.log("play");
    // this.backgnd.show();
    // this.backgnd.size(this.height);
    // this.backgnd.play();
    // this.backgnd.volume(1);
  }
}
